server:
  port: 9999
  servlet:
    context-path: /dynamint
logging:
  level:
    org: 
      springframework:
        security: DEBUG
        web: DEBUG
        data: DEBUG
spring:
  datasource:
    url: jdbc:oracle:thin:@(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=localhost)(PORT=1521))(CONNECT_DATA = (SID = XE)))
    username: DYNAMINT
    password: welcome
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.Oracle10gDialect
  data:
    rest:
      base-path: api/v1
#spring:
#  profiles:
#    active:
#    - sso